type based alias analysis:
    - float* and int* won't ever alias (can reorder)
    - foo* and bar* same thing (both called noalias)
    - int* and int* (may alias)
    - if no subtyping or inheritance (no common ancestor type node check?)
    
memorySSA:
    - memorydef
    - memoryuse
    - memoryphi

escape analyis:
    - if the address of alloca escapes at ANY point in the function
      we should treat the alloca unsafe for promotion / deletion

    - for each alloca, walk it's uses recursively and look for
      store of the alloca ptr, or gep of it into memory or another variable,
      plain gep or other ptr escapes

    - 


SROA (scalar replacement of aggregates):
    - break down small aggregates (structs / arrays) into individual scalar components
    - fields can be stored in registers instead of memory


pre-SSA memory op cleanup:
    - find alloca's that don't escape
    - walk over function and compute index for all instructions (densemap)

    - pre-compute GEN[B] and KILL[B] per block
        - GEN[B] -> true if there is a load that is not preceded by a store
          to that alloca in this block (no store between block entry and load)
        - KILL[B] -> true if there is at least one store to the alloca anywhere
          in the block


    - live[B] = bitset<N>
  




Store-to-load forwarding (we are going to hold off on this pre-mem2reg)
    stl fwd is basically a local mem2reg phase... let's keep the representations distinct phases

1. Constant propagation
2. Constant folding
3. Local CSE



