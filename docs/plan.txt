plan:
    - cooper harvey kennedy dominance algorithm
    - mem2reg (promote memory alloca/load/store to registers)
    - SROA (picks up work mem2reg can't do) (optional? maybe skip for now)
    - optimizations (SCCP -> GVN -> DCE -> LICM/unrolling) (first 3 are top priority)
    - SSA destruction? (or keep in SSA?)
    - pre-ra schedule -> register allocation -> post-ra schedule

Today:
    refactor some Sema (nice node query tuple unpacking structured binding generics (some ideas there...))
    array size correctness
    constness in frontend so we can unlock true constant folding in IR
    opaque ptr type info in IR (info is in operands
    SROA (look at llvm document, vector example)   
